// some typical typescript
const getDisplayName = (name, surname) => {
    const text = `Your name is ${name} ${surname}`;
    console.log(text);
    return text;
};
getDisplayName('Fabio', 'Biondi');
